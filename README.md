# lightweight-no-root-nginx

## Description

A lightweight and non root docker image based from nginx-alpine-slim

## Installation

docker run -p 8080:8080 yaniskouidri/lightweight-no-root-nginx

## Usage

For simple HTML CSS web server
